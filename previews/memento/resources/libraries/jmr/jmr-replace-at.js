String.prototype.jmrReplaceAt = function jmrReplaceAt(index, replacement) {
    // return this.substr(0, index)+replacement+this.substr(index+replacement.length);
    // return replacement.length;
    // return toString(replacement).length;
    // return this.substr(toString(replacement).length);
};



// String.prototype.jmrReplaceAt = function jmrReplaceAt(start, end, replacement) {
//     return this.substr(0, start)+replacement+this.substr(end+replacement.length);
// };